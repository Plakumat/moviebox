import React from 'react';
import { AppTitle } from '../../model/common';

const Footer: React.FC = () => {
  return <footer className='moviebox__footer'>{AppTitle}</footer>;
};

export default Footer;
